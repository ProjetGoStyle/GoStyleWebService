var expect = require('chai').expect;

describe('API REST', function() {
    describe('get coupon', function() {
        it('a', function() {
            expect(2).to.equal(2);
        });
    });
});
